# ==========================================
# mycontracts - Environment Configuration
# ==========================================
#
# Diese Datei zeigt alle verfügbaren Konfigurationsoptionen.
# Die Anwendung funktioniert OHNE diese Datei mit hervorragenden Defaults!
#
# Kopiere diese Datei nach .env und passe die Werte nach Bedarf an:
#   cp .env.example .env
#
# Die .env Datei wird von docker-compose.yml genutzt.
# Für standalone JAR-Nutzung kannst du die Variablen auch direkt setzen:
#   export FILE_STORAGE_PATH=/custom/path
#   java -jar mycontracts.jar
#

# ==========================================
# Datei-Speicherung
# ==========================================

# Verzeichnis für hochgeladene Vertrags-Dateien
# Default: ./data/files (wird automatisch erstellt)
# Hinweis: Nutze absolute Pfade für Produktion
FILE_STORAGE_PATH=/data/files

# ==========================================
# OCR-Watcher
# ==========================================

# Verzeichnis für OCR JSON-Dateien (*_ocr.json)
# Default: ./data/incoming (wird automatisch erstellt)
# Der Watcher überwacht dieses Verzeichnis automatisch
WATCH_DIR=/data/incoming

# ==========================================
# LLM / AI Configuration
# ==========================================
# Siehe docs/FREE-TIER-AI-APIS.md für kostenlose Optionen

# LLM Provider (aktuell nur "openrouter" unterstützt)
LLM_PROVIDER=openrouter

# OpenRouter API Key (optional - ohne AI-Features deaktiviert)
# Registrierung: https://openrouter.ai/
OPENROUTER_API_KEY=

# OpenRouter API Base URL
# Default: https://openrouter.ai/api/v1
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# OpenRouter Modell-Auswahl
# Empfohlene kostenlose/günstige Modelle:
#   - meta-llama/llama-3.2-3b-instruct:free (völlig kostenlos)
#   - google/gemini-flash-1.5 (sehr günstig: $0.075/$0.30 per 1M tokens)
#   - openai/gpt-3.5-turbo (beste Qualität: $0.50/$1.50 per 1M tokens)
#   - anthropic/claude-2 (hochwertig aber teurer)
OPENROUTER_MODEL=openai/gpt-3.5-turbo

# Legacy Gemini Support (deprecated - nutze stattdessen openrouter)
GEMINI_API_KEY=

# ==========================================
# Datenbank
# ==========================================

# SQLite Datenbank-Datei
# Default: ./mycontracts.db (wird automatisch erstellt)
# Hinweis: SQLite ist pure Java, keine Installation erforderlich!
SPRING_DATASOURCE_URL=jdbc:sqlite:mycontracts.db

# ==========================================
# Server-Konfiguration (Optional)
# ==========================================
# Diese können auch via command-line gesetzt werden:
#   java -jar mycontracts.jar --server.port=9000

# Server Port (Default: 8080)
# SERVER_PORT=8080

# Context Path (Default: /)
# SERVER_SERVLET_CONTEXT_PATH=/

# ==========================================
# Erweiterte Optionen (Optional)
# ==========================================

# OCR-Watcher Scan-Intervall in Millisekunden (Default: 5000 = 5 Sekunden)
# WATCHER_SCAN_INTERVAL_MS=5000

# Maximale OCR-Matching Wiederholungsversuche (Default: 5)
# WATCHER_MAX_RETRIES=5

# Retry Backoff in Millisekunden (Default: 5000)
# WATCHER_RETRY_BACKOFF_MS=5000

# Spring Profile (für Entwicklung)
# SPRING_PROFILES_ACTIVE=dev

# Log Level (Default: INFO)
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_DE_FLEXIS=DEBUG

# ==========================================
# Zusammenfassung: Minimale Konfiguration
# ==========================================
#
# Für die meisten Nutzer reicht es, nur diese zwei Werte anzupassen:
#
#   FILE_STORAGE_PATH=/mnt/contracts/files
#   WATCH_DIR=/mnt/contracts/incoming
#
# AI-Features sind optional und erfordern nur:
#
#   OPENROUTER_API_KEY=sk-or-...
#
# Alle anderen Werte haben sinnvolle Defaults!
#
